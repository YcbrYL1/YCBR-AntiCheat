# YCBR-AC 差距审查报告 v2（对照 1.8.9 原版物理 + 参考反作弊源码）

日期：2026-08-11
审查对象：YCBR-AC（82,646B，A/B/C 阶段完成后）
参考源：
- 1.8.9/1.12 原版反混淆源码（Southside MCP 版，基础物理 1.8-1.12 一致）
- Grim-2.0（GrimAC 新版，150+ check）
- NoCheatPlus-master（NCP 经典，1.8 兼容）
- AntiCheatReloaded-master（ACR）
- MX-Project-master（MX 5.4 Requin，1.8 瞄准专精）
- AntiCheatAddition-master（ACA Photon，1.20+）

## 一、判据缺陷与物理漏洞（P0，含误报风险）

| # | 位置 | 问题 | 影响 | 证据 |
|---|---|---|---|---|
| P0-1 | KillAuraCheck.java:27 | **onMove 无 isEnabled 门控**（唯一门控在 onAttack:192）。killaura.enabled=false 时 aim 系（Modulo360/AimStep 收集/Gcd/GcdGrid/ConstStep/AxisAsym/BigRot）照常运行且可 flag | 配置失效 | 对照 TimerCheck:20 / SpeedCheck:19 |
| P0-2 | SpeedCheck.java:23 | **无水中豁免**。1.8 游泳稳定水平速度 = 0.02×2/0.2 = **0.2/tick**（加速度 0.02、阻力 0.8），水流推力再 +0.014/tick；airLimit 的 momentum 衰减至最小值 0.11 → 长期游泳/水流中移动超限 | **绿玩游泳误报** | 原版 EntityLivingBase.travel 水分支 :1755-1757,1899-1930；SpeedCheck airLimit :78-94 |
| P0-3 | FlyCheck.java:36-70 | **Rise 无梯子豁免**。1.8 爬梯合法 motionY 钳制 -0.15~+0.2（水平碰撞+梯子=0.2）；airTicks 持续增长使 expected 衰减至 0.1，爬高塔（bedwars 常见）motionY=0.15-0.2 > expected+tolerance | **绿玩爬梯误报** | 原版 EntityLivingBase.java:1839-1860；MovementTracker 无 ladderTicks 状态 |
| P0-4 | VelocityCheck.java:24 | **无 ping 门控**（其余 check 均有 ping>400）。高 ping 击退应用延迟 → 0.35 比率连续 2 tick 误判 | 高 ping 绿玩误报 | VelocityCheck 门控行 |
| P0-5 | KillAuraCheck.java Reach :276-298 | **实体快照 4 tick（80ms）陈旧且无插值**。目标高速移动（0.5+/tick）时攻击瞬间真实距离已超出快照距离 → 接近 3.05 上限的攻击误报；反之也漏 | 快移动目标误报 | EntitySnapshotService 4 tick 刷新（config.yml:6） |
| P0-6 | GcdStable | std<0.25 使 GcdStable 退化为"恒定步长"检测，与 ConstStep 冗余；真正的 GCD-Sync 网格瞄准由 C1 的 GcdGrid 承担（正确方向）。保留但认知上归入网格判据族 | 冗余（非 bug） | 见 detection-gap-analysis.md GCD 专项 |

## 二、缺失检测类别（P1，1.8.9 可落地、防 LB 价值高、误报风险低）

| # | 检测 | 参考实现 | 判据本质 | 实现量 |
|---|---|---|---|---|
| P1-1 | **WrongTurn（非法 pitch）** | NCP WrongTurn / ACA IllegalPitch +150vl | pitch 超出 ±90° 或 NaN。1.8 服务端只校验坐标有限性（NetHandlerPlayServer:326-336），**不查 pitch** → 协议级铁证，零误报 | 小（LOOK 包读 pitch） |
| P1-2 | **Blink / 位置包超时** | Grim BadPacketsR（2000ms 无位置）/ BadPacketsE（20 tick 上限） | 1.8 客户端站立每 20 tick（1s）强制发一次位置包（EntityPlayerSP positionUpdateTicks≥20）→ 位置包间隔 >1500ms 且非死亡/载具/旁观 = Blink。防 LB Blink 冻结，零误报 | 小（lastPositionTime 窗口） |
| P1-3 | **NoSwing（攻击无挥手）** | Grim PacketOrderB / NCP NoSwing / ACA Animation +30vl | 1.8 顺序 ANIMATION→USE_ENTITY（1.9 相反），攻击后窗口内无 ANIMATION 包 = 静默攻击。LB KillAura 大量静默 | 小（包序状态机） |
| P1-4 | **Sprint 矛盾** | MX Sprint（START/STOP <10ms "Zero's flaw"） | 攻击窗口内 sprint 状态包以 <10ms 间隔翻转（机器特征，人类不可能） | 小 |
| P1-5 | **FastEat / FastBow** | ACR FastEat 1000ms / FastBow bowError 0.25；NCP InstantEat/InstantBow 130ms | 吃喝 <1000ms、拉弓 <130ms = 加速使用 | 中（需 PlayerInteractEvent/物品使用监听） |
| P1-6 | **ClientBrand 指纹** | Grim ClientBrand（>64B 异常）/ ACA Sentinel | 1.8 插件消息 MC\|Brand / FML\|HS 指纹，LB 特征识别 | 小 |

## 三、架构级差异（P2，与现有约束冲突或依赖缺失基础设施）

| # | 类别 | 参考实现 | 说明 |
|---|---|---|---|
| P2-1 | **GhostBlock / 地面声明校验** | MX GhostBlockAbuse（3x3x3 采样）、Grim GroundSpoof、NCP Passable | 需 Bukkit 世界查询。现有约束"检测不做世界查询"，但 MainThreadHandler 已具备方块上下文快照能力（blockOnIce/Slime/Liquid/Web/BoxedIn），可扩展 blockOnLadder/脚下实体方块校验 → 冲突可协商解除 |
| P2-2 | **竖直运动完整包络** | NCP SurvivalFly（重力 GRAVITY_MAX 0.0834/MIN 0.0624、hover、fallingEnvelope 全套） | YCBR Fly Rise+Level 已近似覆盖主要面；下落加速度异常（重力减半类 NoFall）未校验 |
| P2-3 | **Transaction 时钟** | Grim Timer 家族/AntiKB/TransactionOrder、NCP 事务对账 | 1.8 可由 Window Confirmation 承载（Grim 确认），但 YCBR 无事务基础设施；当前墙钟 Timer（6s 窗 eps>22）已满足防 LB，Transaction 仅提升精度 |
| P2-4 | **攻击频率细分** | NCP AttackFrequency（0.5s→10/1s→15/2s→30/4s→60/8s→100） | YCBR Cps 20/s+连续 2 窗已覆盖基本，粒度更细可后置 |

## 四、已覆盖对照（确认无缺口的）

- GCD 众数网格：C1 GcdGrid ✓（Grim AimProcessor 众数桶思想移植）
- 多目标换攻 ✓（Grim MultiInteractA 同理念）、SelfInteract ✓、AimModulo360 ✓（Grim 同判据 320°）
- ExtremeMove ✓（NCP EXTREME 4.0/22.0 同参数）
- Reach ✓（3.05+ping×0.002，NCP 4.4 略严/ACR 3.55+0.2 略严）
- 放置字段铁证 ✓（InvalidPlaceA/B+FabricatedPlace，Grim 同款）
- FastPlace 22/s ✓（NCP limit 22 一致）
- 包频 Timer ✓（NCP EPSMax 22 一致）
- 物理豁免：冰/粘液（slimeTicks≥40）/活塞（0.45 突变+水平静止）/液体/web（Fly）✓；Speed 对活塞经 spike-grace 放行 ✓

## 六、误封风险审计（实测误报后追加，20:06 已修复组）

### 实测误报修复（83,705B）
| 误报 | 根因 | 修复 |
|---|---|---|
| AimModulo360 未攻击触发（yaw wrap 433/-533） | LOOK 包未入流 → prevYaw 冻结 → rawDelta 累计转头量 | 监听 LOOK 包维护 yaw 链 + 前 tick<30° 判据（Grim 同款） |
| GcdGrid mode=0.0 aligned=10/10 | 人类匀速转动 delta 恒定 → gcd=delta 自身 → 众数全对齐 | mode 界 [0.0005°,1.0°] + distinct dots≥3（机器多 k 值） |
| BigRot 正常攻击触发 | 人类 PvP 转身 600ms 4 次 | 默认 5 次/800ms（config 化） |

### 静态误封风险审计（本轮发现并修复）
| # | 位置 | 风险 | 修复 |
|---|---|---|---|
| FP-1 | MovementTracker elapsed 钳制 **5ms** | 低 TPS/客户端追赶发包 → 包间隔 0-5ms → timeScale ×10-50 → Speed/Fly **放大误报** | 钳制 50ms（timeScale≤1，移动量永不放大；>50ms 按比例缩小=宽松方向） |
| FP-2 | Fly Rise `max(1.25, expected)` | 1.25 硬上限架空包络判据：LB 匀速 0.5 上升**漏检**；且无跳提药水加成 → **跳提跳跃误报** | 去掉 1.25 兜底直接用 expected；加 `jumpLevel*0.1` 与击退加成 |
| FP-3 | jumpedThisTick = motionY>0.3 | 起跳后 2-3 tick 仍 true → Rise 豁免窗口过长（配合 FP-2 双重失效） | 改"起跳瞬间"（>0.3 && lastMotionY<0.3） |
| FP-4 | Speed 无水中豁免 | 游泳稳定 0.2/tick > airLimit 0.11 → 游泳误报 | nearLiquidTicks>0 || inWebTicks>0 → return |
| FP-5 | Velocity 无 ping 门控 | 高 ping 击退延迟 → 误判 | 补 ping>maxPing 门控 |
| FP-6 | KillAura.onMove 无 isEnabled | 关检测仍 flag | 补门控 |
| FP-7 | Interval cv<0.15 样本 8 | 人类稳定连点（cv 0.1-0.15）误报 | cv<0.10 + 样本 12（config max-cv） |
| FP-8 | Cps 20/s | butterfly click 20-25cps 误报 | 22/s（config） |
| FP-9 | Scaffold Colinear 纯共线 | **笔直搭路=人类正常行为**，纯共线必误报 | 组合判据：共线 + 50ms 节拍网格（对齐 10ms） |
| FP-10 | Scaffold Grid45 pitch 区间 75-85 | 对角高桥天然 yaw45 网格 + pitch 看脚下 → 误报 | 加 pitch 标准差 <0.15（人类微动 0.2+，机器≈0） |
| FP-11 | Scaffold MovePlace 0.55 | 疾跑跳搭 xz≈0.65 → 误报 | 豁免 jumpedThisTick |

### 遗留误封风险（20:21 已全部修复，除 FP-17）
| # | 位置 | 风险 | 修复 |
|---|---|---|---|
| FP-12 | 方块快照 5 tick + 服务器位置 | 入水/上冰瞬间上下文滞后 250ms+ → Speed/Fly 误报窗口；高 ping 状态滞后 | 快照间隔 5→2 tick（config）；基准改包坐标（volatile lastX/Y/Z） |
| FP-13 | Reach 实体快照 4 tick 无插值 | 快速目标接近 3.05 上限误报 | 快照带速度（采样位移/间隔）+ 攻击时刻外推目标位置，age cap 20 tick |
| FP-14 | 踢出=累计 vl≥kick-at（无时间窗） | 稀疏偶发误报长期累积终误封 | flag 时间窗：同类型 flag 间隔 >60s（config vl-reset-window-ms）重置 VL |
| FP-15 | Scaffold Rotation 100° 连 5 次 | 横桥（侧放）5 连判 | 120°/8 连（config） |
| FP-16 | Cadence 网格 | 人类固定节奏搭路 6 连 50ms 网格 | batch-size 6→8 |
| FP-17 | AimStep 1e-5 | 高 DPI 纯垂直甩动 | 保留（Grim 同款，观察实测） |

### 20:21 顺带修复（P0-3 梯子豁免）
- MainThreadHandler 快照加 `blockOnLadder`（LADDER/VINE 脚部或下方）；MovementTracker 加 ladderTicks(6tick 窗口)；Fly Rise 梯子期间豁免（原版爬梯 motionY≤0.2 合法）

## 七、结算状态（编译验证通过：45 classes，ProtocolLib 5.0.0）

| 条目 | 状态 | 位置 |
|---|---|---|
| P0-1 门控 | ✅ | KillAuraCheck onMove:27-31 |
| P0-2 水中豁免 | ✅ | SpeedCheck:27-29 |
| P0-3 梯子豁免 | ✅ | FlyCheck Rise :47-50 |
| P0-4 ping 门控 | ✅ | VelocityCheck:24-26 |
| P0-5 快照外推 | ✅ | KillAuraCheck checkReach（age cap 20t + 双方 ping 补偿） |
| P0-6 GcdStable 冗余 | ✅ 认知归入网格族 | GcdGrid mode 界 + dots≥3 |
| P1-1 WrongTurn | ✅ | check/protocol/WrongTurnCheck（NaN/超界 pitch，vl-before-flag=1） |
| P1-2 Blink | ✅ | check/protocol/BlinkCheck（>2s 无位置包，TPS≥15 门控，5s 冷却） |
| P1-3 NoSwing | ✅ | KillAuraCheck checkNoSwing（250ms 窗口；独立的 protocol/NoSwingCheck 已删除防重复） |
| P1-4 Sprint 矛盾 | ✅ | check/protocol/SprintCheck（START/STOP <20ms 翻转×3） |
| FP-4 游泳误报 | ✅ | SpeedCheck nearLiquid/inWeb return |
| FP-12 快照滞后 | ✅ | MainThreadHandler 快照 2 tick + 包坐标基准 |
| FP-13 Reach 外推 | ✅ | 见 P0-5 |

### 本轮追加修复（Speed 疾跑跳跃 FP、NoFall 加固、Fly 状态、Criticals 药水）
| # | 问题 | 修复 |
|---|---|---|
| FP-18 | Speed airLimit momentum 0.42（config）< **疾跑跳跃合法水平速度 0.486→0.442→0.402→0.366…**（跳跃+0.2 疾跑加成 + 空气控制 0.02/tick），任何正常跳搭/PvP 疾跑跳空中前 3 tick 必超限误报 | SpeedCheck 新增 burst 曲线：airTicks 1-6 = {0.52,0.49,0.47,0.45,0.44,0.42}×药水因子（(0.286×(1+0.2L)+0.2)/0.486），6 后 ×0.91^t 衰减；config `speed.air.burst-potion-factor` |
| FP-19 | Criticals 无跳提药水补偿：Jump II 跳跃 motionY=0.504 > expectedMax 0.462 → 跳提 PvP 误报 | expectedMax 加 `jumpLevel×0.1`（与 Fly Rise 一致） |
| FP-20 | NoFall 下方方块未校验：clientOnGround×2 + airTicks>8 + motionY<-0.4 在靠墙/斜坡边缘可能误判 | 仅当 `blockBelowUnstandable`（AIR/液体/植物/梯/火把等不可站立）才 flag，否则 drain |
| FP-21 | FlyCheck hoverTicks/lastRiseOver 为实例字段 → **全服共享**，一人状态污染他人 | 迁移到 PlayerData 每玩家字段 |
| GUI | command/GuiManager 与 MainThreadHandler API 未对齐（hasAlert 缺失、openChecks/openCheckSubs 签名） | 补 `MainThreadHandler.hasAlert`；openChecks/openCheckSubs 携带 targetId；PLAYERS 页 Reload/Checks/Alerts/Refresh 按钮接入 |
| 构建 | ProtocolLib 5.0.0 不在 dmulloy2 仓库/GitHub 直连超时 | 从 GitHub API 资产下载，`install:install-file` 装入本地 .m2；pom 固定 5.0.0 |

### 遗留观察（非阻塞）
- FP-17 AimStep 1e-5 纯垂直甩动：保留，待实测观察
- Timer 6s 窗 eps>22：高 ping(400ms) 上限 21.3 不误报；1.5× 倍速必检出。事务时钟（P2-3）留待按需
- NoSwing 归入 KillAura（随 killaura 开关）；如需独立开关可拆回独立检查

## 八、纯审计防误报修正（第二轮，2026-08-11 晚，全部已编译 ✅ 45 class）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| FP-22 | VelocityState.horizontal()/expectedHorizontal() | 用 |vx|+|vz| 曼哈顿和当水平速度 → 击退时 Speed/Velocity 上限虚高、误判方向；且期望方差随方向 | 改欧氏 √(vx²+vz²)，衰减 ×0.91^min(20,ticksSince) |
| FP-23 | FlyCheck Rise slimeTicks | `>= 40` 太薄：40 tick 只覆盖 1.8 格高，黏液 Boost 连续弹跳无限增长仍逃逸 | `> 0` 即豁免（黏液连续 Boost 每次落地都重置） |
| FP-24 | FlyCheck Rise max(0D) | 负期望破坏"跳起首 tick"校验（抬头起跳瞬间合法但期望为负 → 过判） | 下限改 max(0.12D, …) |
| FP-25 | FlyCheck Level hover 判据 | 用服务端 airborne 判悬浮 → 服务端着地检测失误即误报；客户端旗标 onGround 才是原版判定依据 | 改用 `!clientOnGround` 判悬浮 |
| FP-26 | CriticalsCheck 楼梯/台阶 | 徒步上楼梯/台阶（每 tick y 增量 >0.45）合法，jumpLevel 补偿不够 | `|lastY-lastLastY|>0.45 && distanceXZ<0.1` → drain |
| FP-27 | ScaffoldCheck MovePlace | maxSpeed=0.55 无击退/击飞补偿 → 击退中被放方块误报 | 0.55 + velocity.horizontal() |
| FP-28 | EntitySnapshotService 速度 | 假设快照间隔恰为 tick(50ms) → 高 TPS/包波动下速度失真 | 按真实时差缩放：50/(now-createdMillis) |
| FP-29 | KillAura Angle 用 lastYaw 立即判角 | **USE_ENTITY 先于当 tick 的 LOOK 包到达**（客户端每 tick 末才发 C03），甩枪/快速转身攻击时 lastYaw 是上一 tick 的旧值 → 高手甩枪必误报 | 攻击只入队 `pendingAngleTargets`，**下个 LOOK/位置包到达时用其 yaw/pitch 延迟判定**（攻击瞬间真实瞄准）；>400ms 无旋转则跳过（站桩点击合法） |
| FP-30 | FlyCheck Level 梯上静止 | 梯上站立 clientOnGround=false 且 motionY=0 → hover 累加 8 tick 误报 | nearLiquid/inWeb 豁免条件加 `ladderTicks > 0` |

## 建议执行顺序

1. **P0 修复组**（误报风险，直接改）：P0-1 isEnabled、P0-2 水中豁免（nearLiquidTicks>0 → 水中 limit 或 return）、P0-3 梯子豁免（MainThreadHandler 加 blockOnLadder，MovementTracker 加 ladderTicks）、P0-4 ping 门控、P0-5 快照外推（或 allowed 动态 +0.1）
2. **P1 新增组**（防 LB 铁证，一次一个）：P1-1 WrongTurn → P1-2 Blink → P1-3 NoSwing → P1-4 Sprint 矛盾
3. P2 按需协商（P2-1 需解除"无世界查询"约束，P2-3 需引入事务基础设施）

## 九、第三轮 FP 审计修正（2026-08-11 深夜，已编译 ✅）

> 并行会话同时重写了 VelocityCheck（ping 补偿 + 世界查询墙/天花板豁免）、默认禁用全部统计类判据（aimstep/gcd/gcdgrid/conststep/axisasym/interval/multitarget/bigrot 等），本表为与之对齐后的补充修正。

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| FP-31 | SpeedCheck ExtremeMove | 网络停顿坠落 dy>2.5 触发 tracker 传送分支，lastMoveY 仍 >4.0 → 单次 3vl 直接 flag | `initialized` 检查移到 ExtremeMove 之前（任何 >2.5 单包位移都走重初始化，不再误判） |
| FP-32 | SpeedCheck airLimit 冰面 | `iceTicks 1-59` 时冰面加成 ×2.5（上限 0.45），`≥60` 反而只 +0.18 —— 逻辑颠倒，检测空洞 | 统一为 min(0.18, 0.025×1.038^t)；冰面合法速度 0.359 由 momentum 覆盖 |
| FP-33 | CriticalsCheck 黏液弹跳连打 | 黏液弹跳 motionY=1.0 是合法物理，弹跳期攻击必超 0.42 曲线 → 黏液 PvP 误报 | `slimeTicks > 0` → drain |
| FP-34 | CriticalsCheck 蜘蛛网攻击 | 网内 motionY 0.05-0.08 贴近 >0.08 阈值 | `inWebTicks > 0` → drain |
| FP-35 | CriticalsCheck 空中被击飞攻击 | airTicks>6 阈值固定 0.08 无 KB 补偿：空中吃击飞后攻击（PvP 连段常态）误报 | 阈值改 `0.08 + kb` |
| FP-36 | VelocityCheck 坠落中被击飞 | 坠落 -0.35 时 KB +0.42 净运动 0.07 → 17% < 30% → 误报（此前两版均未豁免） | `lastMotionY < -0.05`（KB 前在坠落）→ 豁免 pct 判据 |
| FP-37 | VelocityCheck blockedAbove | 天花板检测只查 feet+2..+3，漏掉头部高度（feet+1）方块：2 格高隧道/半砖天花板下吃 KB 被判"完全吸收" | 改 `dy = 1..3` |
| FP-38 | KillAura Reach | 目标停止/反向时外推速度过期 → 距离虚高（追尾命中末端误报）；双方对冲时快照陈旧 3 tick 同样虚高 | `reached = min(外推, 原始快照)`：外推修正对冲、原始快照修正急停，两者同时高估不可能（目标接近⟹外推正确，目标停止⟹快照正确）→ 证明 0 误报 |
| FP-39 | KillAura Angle 甩枪后判角 | 攻击后下一个 LOOK 是转身后视角（甩枪/走位连点 5 击期间视角已移开）→ 误判"不在准星" | 记录攻击时刻 yaw，flush 时 `|Δyaw| > angle.turn-exempt-degrees(25)` → 跳过该攻击（视角不稳无法验证） |
| FP-40 | KillAura AimStep 水平 flick | 高手水平甩枪 pitch 精确锁 0°（鼠标只动横向），dPitch=0 每 tick 触 step → vl 8 误报 | 需**连续 3 tick** 同轴锁定才 bump（人类 flick 间 pitch 必然漂移） |
| FP-41 | Scaffold 空气右键 | 近出生点（20 格内）右键空气发 C08(0,0,0,face=255) → PlaceAim "block unreachable" 误报；右键空点也计入 fastPlace | face==255 → 仅校验字段后直接 return |
| FP-42 | Scaffold MovePlace 药水 | 疾跑跳落地 tick distanceXZ=0.49，Speed I 时 0.588 > 0.55 误报 | `+ speedLevel×0.1` |
| FP-43 | Scaffold Cadence/Colinear | **所有 tick 对齐的人类匀速搭路都会被判违规**（点击天然落在 50ms tick 边界，间隙恒为整数 tick）——god-bridge/直线桥高手必误报 | 要求间隙 tick 数**恒定**（固定 2 tick 等），人类节奏 ±1 tick 漂移即安全；仅机器固定间隔命中 |
| FP-44 | TimerCheck 服务器卡顿 | 服务器 CPU 饱和后 netty 恢复瞬间包突发 → 6s 窗 eps 冲高误报 | TPS < 15 门控（与 Blink 一致） |
| FP-45 | config 参数 | place-aim.max-vertical 4.0 卡 MLG 救援放置边缘（dy≈-4.1）；blink 2000ms 不抗 wifi 抖断 | max-vertical 4.5、max-silence 3000、cadence batch-size 10 |

## 十、第四轮 防绕过修正（2026-08-12，已编译 ✅）

> 前两轮为 0 误判放宽的判据被外挂反向利用（"外挂不也可以完美绕过了吗"），本轮按 0 误判 + 100% 检出双约束封堵绕过面。统计类判据经确认只启用**数学上人类无法满足**的安全子集。

| # | 位置 | 绕过面 | 修复 |
|---|---|---|---|
| BY-1 | SpeedCheck ExtremeMove | 旧判据横向阈值 22.0 且放在 initialized 之后：任何 >3.0 单包水平位移都被 MovementTracker 当"传送"重初始化 → phase/对角 blink（每 2s 一次、<BlinkCheck 静默窗）零成本穿墙 | 相位判据移到**重初始化 tick（!initialized）**上：水平 lastMove >3.0 且无 0x08（`lastTeleportTime` 1s 窗，与 BlinkCheck 一致）且无 velocity.pending（防叠 TNT/强击退 3.5 格单包合法位移）→ flag phase；纯垂直大位移（坠落/黏液弹射）天然豁免 |
| BY-2 | VelocityCheck 坠落豁免 | FP-36 的 `fallingBeforeKb` 完全跳过 pct 判据：空中吃 KB 时"全取消击退"挂（motionY 维持坠落负值）零成本绕过 | 豁免改为**上升确认宽限**：`fallKbMissTicks` 计数，KB 前在坠落且 motionY<0.05 连续 2 tick → flag "KB canceled while falling"；真实延迟 tick（KB 晚一 tick 生效，motionY 次 tick 升到 0.4）自然清除计数，0 误报 |
| BY-3 | KillAura Reach 快照陈旧 | 快照 4 tick 刷新，追逃杀时目标位置滞后 ≤3 tick（≈0.85m 疾跑）→ min(raw,外推) 中被低估一侧被利用，贴脸 3.05m 判距实则 4m+ 命中 | 玩家目标改用**目标自己的 MovementTracker**（每 tick 刷新，≤1 tick 新鲜）重建快照并 ageTicks=0；非玩家目标保留原 min(外推,raw) 行为 |
| BY-4 | config 统计判据全禁 | 并行会话默认禁用 aimstep/gcd/gcdgrid/conststep/axisasym/interval/multitarget/bigrot/cps/modulo360 → aimbot 零成本 | 经确认启用安全子集：**aimstep、gcd、gcdgrid、conststep、interval、grid45、duprot**（人类无法自然满足的数学特征）；cps/multitarget/bigrot/axisasym/modulo360 保持禁用（高手高敏操作存在误判边界） |

## 十一、第五轮 防绕过修正（2026-08-12，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗" → 逐条复核上轮放宽点与并行会话新增内容，本轮 6 处修正（含 1 处**既有误报**：NoFall 梯子下滑）。

| # | 位置 | 绕过/误报面 | 修复 |
|---|---|---|---|
| BV-1 | VelocityCheck 空中全取消 | **BY-2 只覆盖 motionY∈(0.01,0.05)**；分支入口 `motionY>0.01` 使"空中吃 KB 后完全取消击退、维持坠落负值"的挂（最普遍配置）从不进入判定 → 零成本 | 分支改为 `!onGround && !ceiling && !jumpedThisTick`，新增 `motionY ≤ 0.01` 的**全取消路径**：连续 2 tick（容忍 ping 迟 1 tick 生效）→ flag "KB canceled airborne"；onGround/兜底分支同步清零 fallKbMissTicks |
| BV-2 | SpeedCheck spike-grace | 任何单 tick +0.25 加速度都 drain+return → **渐变加速挂（每 tick +0.24 匀速爬升）永远被豁免**，无限提速 | 连续豁免上限 3 tick（config `spike-grace-ticks: 3`）：合法来源（击退=limit 已含、活塞推 ≤3 tick）不超；第 4 tick 起走正常 speed 判据（此时速度必超限 → 命中） |
| BV-3 | FlyCheck Rise 慢飞 | `max(0.12)` 下限 + 0.05 容差在 airTicks 高段恒定存在 → **0.15-0.17/tick 持续爬升（≈3 格/秒慢飞）永久免检** | `airTicks ≥ 45`（2.25s，合法跳跃弧早已结束）后去掉衰减/下限/跳级，只留 `velocity.vertical() + 0.05`（+近水 0.1 覆盖游泳上浮 ~0.11）；任何持续上升在此后必命中 |
| BV-4 | KillAura Angle 快照陈旧 | angleHit 仍用 4-tick 旧快照：对移动目标（尤其对冲）射线打空 → 高手高速格斗误报，且弱化检出 | 与 Reach 同一换法：玩家目标改用其**自身 MovementTracker**（≤1 tick 新鲜）重建快照再打射线 |
| BV-5 | KillAura ThroughWalls（并行会话新增） | `isOccluding() \|\| isSolid()` 兜底 → **玻璃/树叶/蜘蛛网/栅栏等合法隔击**（1.8 原版无视线判定、隔墙命中是高端玩家常规操作）双射线命中即 flag → 误报面大；且未经验证 | 射线只认 **occluding**（不透明实块）；`enabled: false` 默认禁用，待服务器实测验证后再启用（代码保留） |
| BV-6 | NoFallCheck | (a) **既有误报**：梯子快速下滑合法（客户端 onGround=true 支持判定），airTicks>8 且下方是梯子块"不可站立" → 直接 flag；(b) **绕过**：隔 tick 交替报 onGround（50% NoFall）绕开 current+prev 双 tick 门控 | (a) `ladderTicks > 0` → drain；(b) 新增**坠落窗比例**：airTicks>8 且下落时累计 `window-ticks(20) tick` 内 onGround 声明 ≥ `window-min-claims(8)`（合法=0%）→ flag；窗口满即重置 |

## 十二、第六轮 防绕过修正（2026-08-12，已编译 ✅）

> 三问"外挂不也可以完美绕过了吗"后，逐一复核 TimerCheck/BlinkCheck/WrongTurnCheck/SprintCheck/ScaffoldCheck/MainThreadHandler/AsyncPacketListener/CheckRegistry，确认无其他惰性路径；本轮修复 2 处：
| # | 位置 | 绕过/惰性点 | 修复 |
|---|---|---|---|
| BW-1 | KillAura Interval | `attackIntervals` 上限 16（`size() > 16 → remove(0)`）而 `checkInterval` 要求 `gaps.size() >= 20` → **该检出永不可能触发**，interval 挂零成本 | 上限改 24（n 覆盖 20-24）；窗口仍够 0.5s，CV 判据不受影响 |
| BW-2 | VelocityCheck 垂直部分取消 | 上轮只封了 motionY ≤ 0.01（全取消）与 motionY < 0.05 坠落中（miss 路径）；**部分取消**（如 50% 取消 → motionY 0.2、pct 50%）落入 else 重置、永不 flag；且 minimum 30% 以下都被当作"可接受" | (a) `motionY >= 0.05 && pct < minimum` 直接 flag（不分 fallingBeforeKb）——1.8.9 KB 是覆盖不是叠加，合法 pct ≈ 100%，无任何合法路径到 < 60%；(b) `vertical.minimum-percentage` 30 → 60（代码默认 + config）；(c) 坠落 miss 阈值 2 → 3 tick（±1 容差外仅 +50ms 延迟，全取消挂 3 tick 后必 flag） |

> 本轮结论：垂直 KB 取消面已闭环——全取消（miss 路径）、部分取消（pct 判据）、坠落中（并入 pct 判据）全覆盖；水平/横向 ratio 判据存在固有模糊（疾跑 + 水平全取消 ratio ≈ 0.28/expected 恒 > 0.15/0.25 阈值），原理上无法与合法疾跑区分，接受为边界（人类化随机间隙 scaffold 同理）。

## 十三、第七轮 复核既有检测的不足（2026-08-12，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗"后，通读全部 15 个 Check 的现状实现（Fly/Speed/Velocity/NoFall/Timer/Blink/Sprint/WrongTurn/Criticals/KillAura/Scaffold + Tracker/VelocityState/Registry），本轮结论分三部分：**2 处绕过修复、2 处误报源关闭（默认禁用）、1 项物理事实勘误（无需改动）**。
| # | 位置 | 不足/误报源 | 修复 |
|---|---|---|---|
| BX-1 | FlyCheck Rise 慢速爬升 | airTicks≥45 后期望 `velocity.vertical()+0.05`，**持续 0.03-0.05 b/t 上升（0.6-1 格/秒慢飞）无限期免检**——45 tick 后合法上升源只有 velocity（slime/梯子/水已分流），0.05 容差纯属绕过窗口 | 分段容差：airTicks<45 保持 `rise.jump-tolerance: 0.05`（覆盖跳弧模型噪声），≥45 用新 `rise.full-tolerance: 0.02`——合法在 45+ 时 motionY 必为负（坠落），慢飞 ≥0.03 必命中；结合 Level(hover) 判据，0.02-0.05 区间慢上升/慢下降全平面闭环 |
| BX-2 | KillAura MultiInteract 静止误报 | **静止站立玩家**（无移动包，vanilla 仅 1s 心跳 C03）在 1s 窗口内点击两个目标：positionCount 不变 → 直接 bump → **合法门战守点玩家误报**；killaura 双目标实为同 tick（≤50ms） | 加 `multiinteract.max-gap-ms: 100`：两次攻击间隔 >100ms 时视为人类点击（不满足同 tick 特征）不判；killaura 双目标命中窗口 ≤50ms 不受影响 |
| BX-3 | KillAura Post 静止误报 + 时序依赖 | (a) **静止攻击者**：无移动包 → 下一次位置心跳 ~1s → elapsed 远超 max-gap 100ms → 每轮战斗 bump → 误报；(b) 移动者攻击后到下个位置包的间隔依赖客户端发包时序（[0,50]ms 均匀分布，40% 落入 <40ms 被记违规），**无法在不实测客户端行为下保证 0 误报** | (a) 代码：`distanceXZ < 0.02 && lastDistanceXZ < 0.02`（静止）→ drain 豁免；(b) `enabled: false` 默认禁用（与 throughwalls/cps 同策略：代码保留，需服务器实测 vanilla 1.8 客户端攻击-位置同步时序后由服主决定启用） |
| BX-4 | KillAura AutoBlock blockhit 误报 | 1.8 **blockhit（持右键格挡 + 左键攻击）是合法高端技巧**，且 `usingItem` 持续 → 每次攻击 bump（vl-before-flag 1，瞬时 1 vl）→ 逐帧格挡玩家必误报 | `enabled: false` 默认禁用（与 BX-3 同策略，代码保留） |

> 物理勘误：原担心 `ground.limit: 0.34` 误伤 45° 疾跑（凭印象 0.396 b/t = 5.612×√2）。经 mcpk.wiki 公式核验：1.8 的 45° 疾跑乘数为 1.0（直行为 0.98），**对角疾跑稳态 ≈ 0.286 b/t（仅 +2%）**，wiki 传输表：对角行走 4.405 vs 直行 4.317；0.396 是把疾跑 5.612×√2 的错觉。结论：ground.limit 0.34（+0.05/级药水、landing-bonus 0.1、ice/boxed 乘数）**无误报、不须改动**。
>
> 复核确认无需改动的项：Timer（>1.09× 定时器为固有边界，与 1.0-1.05 微小提速不可分）；Blink 3s（vanilla 1s 心跳 + 冻结容差，足够）；NoFall 水底/梯子（motionY < −0.4 判据天然排除水流沉降 ~−0.35）；Criticals 空气判据（slime/水/梯子已分流，ladder 爬速 < 跳弧模型不越界）；Speed spike-grace（第 4 tick 必命中）；Sprint/WrongTurn/interval（上限 24 已修复）；水平 KB ratio（疾跑对冲向量抵消与"取消击退"不可分——接受边界，维持文档结论）。

## 十四、服主决策调整（2026-08-12）

| 项目 | 调整 | 说明 |
|---|---|---|
| ThroughWalls | `enabled: false` → `true` | 服主确认开启。判定保持 occluding-only 射线（玻璃/树叶/栅栏等非不透明隔击不判）+ 双射线（当前/上一 tick）+ 1.5m 最小距离 + 目标速度 ≤0.6 + TPS≥15 + 12s 窗口 ≥2 次才累 VL——符合 0 误判约束 |
| GcdGrid | `enabled: true` → `false` | 服主判断有误判风险，关闭。统计安全子集更新为：**aimstep/gcd/conststep/interval（killaura）+ grid45/duprot（scaffold）**；gcdBucket 数据采集代码保留（`gcd` 本身仍启用，仅 gcdgrid 模式判定停用） |

## 十五、第八轮 复核既有检测的不足（2026-08-12，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗"后，重点复审：ThroughWalls 启用后的误报面、spike-grace 的周期利用、Timer 窗口平均化利用、Speed 逐 tick 判据的振荡绕过。本轮 3 处修复 + 3 项边界结论。
| # | 位置 | 绕过/误报点 | 修复 |
|---|---|---|---|
| BX-5 | SpeedCheck 振荡加速 | spike-grace 每次"干净 tick"后**完全清零** → 交替 0.5/0.2 b/t 的振荡挂：每个尖峰都是"新一轮第一次"到期免 → 永不命中，2 tick 均速 0.35（合法 0.28 的 1.25×）**无限期通过** | 新增**4-tick 滑动窗口**：`sum4 > 4 个逐 tick limit 之和 + min-avg-overage(0.06)` → flag "SpeedAvg"。逐 tick limit 已含落地/跳跃/KB/冰/黏液/箱区逐级修正，合法 sum4 由构造 ≤ limit 之和（KB 时 4×0.76、落地期 4×0.44 均不越界），0 误报；振荡挂 4 tick 内必命中（PlayerData 新增 speedLimits/speedSamples 环形缓冲） |
| BX-6 | TimerCheck 窗口平均化 | 6s 固定窗口被"爆发-停顿"模式利用：2s 内 30/s 爆发 + 4s 停顿 → 6s 均 eps=10 < 22 通过，实际均速 1.25× | 新增 **2s 子窗口**：`epsShort > max-eps-short(24)` → flag "TimerShort"。合法上限：400ms ping 突发到达 ≈ 22.5/s（netty 突发只是聚合不增频），24 有裕量；≥1.2× 持续 2s 必命中；1.15× 以下留作边界 |
| BX-7 | ThroughWalls（本轮启用）误报面收紧 | 1.8 原版近战有 LOS 射线：**合法隔墙打只发生于目标判定箱透过 ≤1 格薄墙**且攻击者贴墙（blockedAt < 1.0）；原 `min-blocked-distance: 1.0` 对"退后 1-1.5m 隔 1 格墙打架"的场景可误报（12s 双命中即累 VL） | `min-blocked-distance: 1.0 → 1.5`：贴墙/薄墙洞 1.5m 内全豁免；killaura 隔墙锁定远距离（3-5m）不受影响，仍由 occluding-only 双射线 + 12s 窗双命中 + TPS 门控约束 |

> 本轮边界结论（接受并记录）：
> (a) **水中 Speed 不检**：nearLiquid → 跳过（游泳上限 ≈ 0.15 b/t，水中提速收益极低，且液体摩擦模型与地面不同，判据需独立标定——维持跳过）；
> (b) **坠落中水平 KB 取消**：`motionY < −0.05` 时水平判据跳过（坠落中垂直判据仍闭环：miss/pct 双路径覆盖），水平分量与坠落方向合成不可分——接受；
> (c) **人类化 aimbot**（随机间隙点击 + 抖动旋转 + 手腕级瞄准误差）与高手操作统计不可分——接受（gcd/conststep/interval/grid45 的启用集已是最优安全子集）。

## 十六、第九轮 复核新增检测与遗留误报面（2026-08-13，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗"后，本次重点：并行会话新增的 6 个检测（NoSlow/AutoTool/FastThrow/Bow/FastClick/BadPacket）无配置即启用（`enabled()` 缺失 key 默认 true）——逐一审计判定安全性与有效性；并复核 NoFall 坠落判据的水域落点。共 2 处修复 + 6 项审计结论 + 3 项边界记录。

| # | 位置 | 绕过/误报面 | 修复 |
|---|---|---|---|
| BY-1 | NoFall 坠落判定 FP | 合法入水落地（水桶 MLG、悬崖跳河、跑酷水坑）无坠落伤害 → 误报 "fell X without damage"。原因：近液守卫（32-34 行）只重置 maxY/minY，空中段 maxY/minY 全程记录，落地瞬间 lastAirTicks>0 → 3.5+ 格无伤坠落判定触发；slime/web 有守卫而水没有 | 落地评估加 `m.nearLiquidTicks == 0`（NoFallCheck.java:46）：落地时仍在水中（含 8 tick 衰减窗口）→ 完全免于坠落评估；水 MLG/跳河/水上漂全豁免，同时坠落时进水的合法玩家不受影响 |
| BY-2 | FastThrow 滞后突发 FP | 网络延迟突发（lag spike/区块恢复）将缓冲的投掷包同 tick 送达 → 50ms 窗内 3 次投掷即 flag（vl-before-flag=1，无 ping 门控）→ 合法玩家雪球大战+延迟即可被踢 | 三重加固：代码加 `max-ping: 300`（高 ping 清零不计数）；`count: 3→4`；`vl-before-flag: 1→3`（需 3 次独立突发且间隔 ≥5s 冷却 → 合法玩家需 15s+ 持续延迟投掷才得 1 次 flag，踢出需 20 次 = 实际不可能） |

> 并行新增检测审计结论（原无配置段即启用，逐一核验后全部保留并补配置段）：
> (a) **NoSlowCheck：数学惰性，安全**——`expected = prev×0.546 + 0.296`，每 tick 预算 0.296 吸收 ≤0.42 b/t 一切速度（含 NoSlow 满速疾跑 0.28），仅在 >0.42 加速时命中（SpeedCheck 已覆盖）→ 不误报也不虚报，保留；
> (b) **AutoToolCheck：安全**——3 连击 + 150s 冷却；人类"切工具→挖掘"最小间隔 >50ms；
> (c) **FastClickCheck：安全**——200ms 窗 >6 击 = 30cps（人类上限 12-14cps）+ max-ping 200 门控；
> (d) **BowCheck：模型核对通过**——800ms 满充模型 vs 1.8.9 真实 1000ms（20 tick 满充），`-130ms` 容差吸收模型偏差，全力度合法拉弓余量 60-260ms 均通过；瞬时满力释放（400ms 内 f=1.0）必命中——检测有效且 0 误报；
> (e) **ProtocolCheck（BadPacket）：全为 vanilla 不可能包**——非法坐标（NaN/±inf/绝对值 ≥2^31）、held-slot 越界（<0 或 >8）、挖掘 face 越界（0-5 外）、keep-alive 重复 id/零 id、载具输入 >0.98、未骑乘 3 连骑跳——原版客户端均不可能产生，安全；
> (f) 六个新增检测的 `checks.*` 配置段已存在，参数与审计结论一致（fastthrow 段已按 BY-2 加固）。

> 本轮边界记录：
> (a) **FastThrow 检测下限**：恰好 20cps（50.0ms 精确间隔）投掷宏可绕过 `<50ms` 判据；>21cps 必命中，≤20cps 的投掷无实战收益——接受；
> (b) **Velocity 箱区跳过**：boxedIn（2 格高通道）时 VelocityCheck 整体 return——通道内 KB 取消无法检出（与 Speed 的 boxed-multiplier 同因），FP 保守优先——接受并记录；
> (c) **turnYaw 死写入**：`lastYaw = fYaw` 在 actor.submit 前赋值 → actor 内 `normalizeYaw(fYaw − lastYaw)` 恒为 0，lastBigTurnTime 写入无效值——无任何读取点，纯死代码，无害。


## 十七、第十轮 复核既有检测的逻辑漏洞（2026-08-13，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗"后，重点复审：KillAura switch 判据的 5ms 窗口是否存在死条件、NoFall onLook 的时序安全性、VelocityCheck 垂直判据的可收紧空间、NoSlow/usingItem 状态链路完整性、以及 interval 与 FastClick 共享 attackTimes 的数据碰撞风险。共 1 处修复 + 6 项审计结论。

| # | 位置 | 绕过/误报面 | 修复 |
|---|---|---|---|
| BZ-1 | KillAuraCheck.checkSwitch | `lastTargetSwitchTime = now` 在 switchMs 计算**之前**赋值 → `switchMs = now − now = 0` 恒为 0 → "5ms 窗口"是死条件（永远通过），唯一判别只剩 `yawRate > 15°` → 一个快速转身双目标近战的合法玩家（如刷怪笼多目标挥砍）只要 yawRate > 15° 即被 flag，而单目标 killaura 以 5ms 间隔打两个不同目标（非法多目标攻击）却无法被"5ms 内连续切换不同目标"的正确签名识别 | 重排代码：先用 `switchMs = now − lastTargetSwitchTime` 取上一次切换的"年龄"，**再**更新 `lastTargetSwitchTime = now`。修复后：flag 条件变为"上一次切换距今 ≤ 5ms（即 5ms 内连续两次攻击不同目标）"——合法人类不可能（两次点击最小间隔 ~50ms），killaura 双目标同 tick 攻击必然命中；同时保留 yawRate > 15° 的额外判别（防止单目标旋转不被误报） |

> 本轮审计结论：
> (a) **NoFall onLook 时序安全**：handlePosition 与 handleLook 共用同一 actor（per-player 单线程 FIFO）；落地同 tick 的 C06 仅触发 handlePosition（不调 onLook），onLook 仅由 LOOK-only 包触发（落地后 airTicks 已归零）→ 不存在"落地同 tick onLook 误报"窗口——安全；
> (b) **VelocityCheck 垂直 60% 下限不可抬高**：1.8 KB 测量存在 ±1 tick 的 ping 对齐抖动（客户端应用 KB 的时机受 ping/2 影响），最差情况 pct = 75%（t=1 测量偏差）；抬至 80 会在 ping 抖动时误报合法玩家；当前 60% 已是抖动下限 75% 的安全裕量——维持 60%，60-79% 拦截率灰区接受；
> (c) **NoSlow 检测完全惰性**：`usingItem` 仅由 handleBlockPlace 设为 true（放置方块时），而实际的 USE_ITEM 包（吃东西/拉弓/格挡）**未在 ProtocolLib 订阅列表中** → `usingItem` 永远反映的是放置而非使用 → NoSlow 的 `!data.usingItem → return` 永远在真实使用物品时退出 → 检测从未生效。因 NoSlow 的预算模型本身已是数学惰性（0.296 吸收 ≤0.42 b/t 一切速度），故即使修正订阅也无实际检测增益——接受现状（文档记录）；
> (d) **WrongTurnCheck 仅做 pitch 合理性检查**（NaN / > 90° / < -90°）——不涉及运动方向比对，检测面极窄但 0 误判——接受（检测目标已由 switch/reach/angle 覆盖）；
> (e) **attackTimes 数据无碰撞**：interval 使用独立的 `attackIntervals`（ArrayDeque, cap 24）；cps 使用 `attackTimes`（ConcurrentLinkedQueue, 1s 窗口）；FastClick 共享 `attackTimes` 但 cleanup 窗口同为 1s，且 registry 迭代顺序保证 cps 先于 FastClick 处理——两者 add/remove 对称，无数据截断风险；
> (f) **BZ-1 修复后 switch 判据的完整签名**：20+ 攻击样本（useEntityCount 未超 40） + 85%+ 为攻击（ratio > 0.85） + 上一次切换 ≤ 5ms（两次攻击不同目标间隔 ≤ 5ms，合法不可能） + yawRate > 15°（最后一 tick 有明显转向）——四重条件缺一不可，0 误判保证，且覆盖 killaura 的核心"瞬间切换目标"特征。

> 本轮边界记录：
> (a) **Velocity 60-79% 灰区**：反 KB 挂保留 60-79% 垂直 KB 的行为不被检测（与水平 KB 比率的 0.25/0.15 灰区同理），0 误判优先——接受；
> (b) **NoSlow 完全失效**： sprint-eat/block-cheat 的移动加速不被检测——该挂件在 1.8 服务器的实战收益有限（吃东西减速仅影响 PvP 中的回复速度，格挡减速仅影响近战防御），且 NoSlow 的数学模型本身无法区分合法减速与非法加速——接受并记录；
> (c) **WrongTurn 仅 pitch 合理性**：一个 killaura 如果 pitch 正确（±90° 内）且 yaw 跟随目标——wrongturn 不触发，由 angle/reach/switch 覆盖——接受。


## 十八、第十一轮 复核并行新增检测与 throughwalls 射线盲区（2026-08-13，已编译 ✅）

> 再问"外挂不也可以完美绕过了吗"后，重点复审：并行会话新增的 ReachCheck（独立检测类）、VelocityCheck 新子项（jumpreset/sprintreset/precise/partial/reversed）、CriticalsCheck 重新启用、throughwalls 射线遍历的 0.5m 盲区。共 2 处修复 + 6 项审计结论。

| # | 位置 | 绕过/误报面 | 修复 |
|---|---|---|---|
| CA-1 | throughwalls 射线遍历 | `t >= len - 0.5D` 导致距目标 ≤0.5m 内的墙块被跳过不检测——killaura 可在目标身后0.5m 内放置方块（如 target 在 5.0m 处、墙在 4.7m 处），射线在 4.5m 停止采样，墙被完全绕过 | 移除 `if (t >= len - 0.5D) break` —射线遍历至目标命中箱边缘，所有遮挡墙块均被检测；目标本身为实体（非方块），不会产生 self-block 误报 |
| CA-2 | VelocityCheck horizontal reversed-streak | `reversed-streak` 为 2（100ms 对冲 KB 方向移动即 flag）——lag spike 瞬间可导致玩家位移方向与 KB 方向短暂相反（1-2 tick），造成误报 | config 新增 `reversed-streak: 3`；代码读取 `si("horizontal.reversed-streak", 3, 2)`；需连续 150ms 对冲方向移动才 flag，lag 瞬间 ≤2 tick 不触发 |

> 本轮审计结论（并行会话新增组件）：
> (a) **ReachCheck（独立检测类）**：与 KillAuraCheck 内部 checkReach 互补——ReachCheck 在每次 onAttack 触发（不依赖 pendingAngleTargets），且对实体位置不做 extrapolation（直接用 snapshot 位置），同时引入 attackAllowance（基于攻击者最近移动距离）+ victimAllowance（基于目标速度和 snapshot 年龄）+ 0.03 leniency——有效范围 ≈3.13m（静止-静止）至3.53m（双方均高速移动）——0 误判（合法高速战斗的位移补偿）；killaura 内部 checkReach 作为备用（extrapolation + ping-comp）双重覆盖；
> (b) **Velocity jumpreset**：检测"受击 tick 内跳跃"的反 KB 模型——需 `vs.y() >=0.3`（有效垂直 KB）+ `groundTicks >=2`（非刚落地）+ `jumpedThisTick` + `kbDelayMs <=40ms`——合法玩家受击后跳跃的延迟 >60ms（人类反应 + 网络延迟）→ 0 误判；streak 3 = 连续 3 次受击跳tick 才 flag——保守设计；
> (c) **Velocity sprintreset**：检测"受击前 60-150ms 停止疾跑"的 w-tap 反 KB 模型——人类反应时间 >100ms，60-150ms 窗口覆盖合法 w-tap（PvP 常规操作）与自动化 w-tap 的重叠区；streak 3 = 连续 3 次精准停顿才 flag——误报风险低（自动化 w-tap 的时间精度 ±5ms，人类 ±30-50ms）；
> (d) **Velocity precise**：检测 ratio 在 [0.97,1.2] 区间且 <0.995 的微弱水平 KB 减少——需 kbPreSpeed <0.06（静止受击）+ timeScale ∈ [0.9,1.1]（正常时间流速）——精准打击"保留 97-99.5% 水平 KB"的反 KB 挂——streak 2 保守；0 误判（合法静止受击 pct ≈100%±2%，低于0.97 需人为干预）；
> (e) **CriticalsCheck 重新启用**：jump-tolerance 0.05，airTicks ≤6 时 expectedMax ≈0.47（含跳跃衰减+KB+跳跃等级），airTicks >6 时 motionY >0.08+KB 即 flag——1.8 合法暴击跳跃 motionY 最大 ≈0.42（airTicks=0）+0.05 容差 =0.47→恰好通过；空中悬停 >6 tick 后 motionY 必然 <0.08→通过；豁免充分（liquid/slime/web/ladder/teleport 1s/velocity pending <20）——0 误判保证；
> (f) **NoSlow 参数变更**（deceleration 0.546→1.0，per-tick-budget 0.296→0.03）：新参数使 NoSlow 从"数学惰性"变为"有效检测"——`expected = lastDistanceXZ ×1.0 +0.03`——一个 sprint-while-eating 的 NoSlow 挂：0.28 >0.03+prev×1.0（prev≈0.11 →0.14）→ 检测命中；合法吃东西减速 ≈0.11→0.14 <0.28→通过——有效检测且0 误判。

> 本轮边界记录：
> (a) **vertical.minimum-percentage 45%**：合法 pct 下限 ≈75%（±1 tick 对齐抖动），45% 远低于此，0 误判保证；但反 KB 挂保留50%垂直 KB 不被检出（原 60% 可检出）——并行会话有意取舍（配合 jumpreset/sprintreset 覆盖特定模式），接受并记录；
> (b) **sprintreset 60-150ms 窗口**：人类合法 w-tap（PvP 常规操作）落入此窗口→不 flag（streak 0 reset）；自动化 w-tap 精准落入→streak 3 才 flag——检测下限为连续 3 次精准 w-tap，单次不触发——接受；
> (c) **throughwalls 移除 0.5m 盲区后**：目标恰好贴墙（目标 hitbox 与墙块相邻）时，射线在目标位置检测到墙→blockedAt ≤0.1m < min-blocked-distance 1.0m→return（豁免）——合法贴墙攻击不误报。

